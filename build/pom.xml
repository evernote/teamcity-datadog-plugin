<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <artifactId>teamcity-datadog-plugin</artifactId>
    <groupId>com.evernote</groupId>
    <version>1.0</version>
  </parent>
  <artifactId>build</artifactId>
  <packaging>pom</packaging>
  <properties>
    <maven.build.timestamp.format>yyyyMMddHHmmss</maven.build.timestamp.format>
    <teamcity.teamcity-datadog-plugin.plugin.version>
      ${project.version}-${maven.build.timestamp}
    </teamcity.teamcity-datadog-plugin.plugin.version>
    <teamcity.teamcity-datadog-plugin.plugin.vendorName>Evernote
    </teamcity.teamcity-datadog-plugin.plugin.vendorName>
    <teamcity.teamcity-datadog-plugin.plugin.vendorUrl>
      https://stash.ba1.etonreve.com/users/tbasanov/repos/teamcity-datadog-plugin/browse?at=refs%2Fheads%2Fdevel
    </teamcity.teamcity-datadog-plugin.plugin.vendorUrl>
  </properties>
  <dependencies>
    <dependency>
      <groupId>com.evernote</groupId>
      <artifactId>teamcity-datadog-plugin-server</artifactId>
      <version>1.0</version>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <plugin>
        <groupId>com.google.code.maven-replacer-plugin</groupId>
        <artifactId>replacer</artifactId>
        <version>1.5.3</version>
        <executions>
          <execution>
            <phase>process-sources</phase>
            <goals>
              <goal>replace</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <file>${basedir}/../teamcity-plugin.xml</file>
          <outputFile>${basedir}/target/teamcity-plugin.xml</outputFile>
          <replacements>
            <replacement>
              <token>@Version@</token>
              <value>${teamcity.teamcity-datadog-plugin.plugin.version}</value>
            </replacement>
            <replacement>
              <token>@VendorName@</token>
              <value>${teamcity.teamcity-datadog-plugin.plugin.vendorName}</value>
            </replacement>
            <replacement>
              <token>@VendorURL@</token>
              <value>${teamcity.teamcity-datadog-plugin.plugin.vendorUrl}</value>
            </replacement>
          </replacements>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-assembly-plugin</artifactId>
        <version>2.4</version>
        <executions>
          <execution>
            <id>make-assembly</id>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
            <configuration>
              <finalName>teamcity-datadog-plugin</finalName>
              <outputDirectory>${project.parent.build.directory}</outputDirectory>
              <appendAssemblyId>false</appendAssemblyId>
              <descriptors>
                <descriptor>plugin-assembly.xml</descriptor>
              </descriptors>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
